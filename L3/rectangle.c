#include <stdio.h>
main(){
	int l,w,area;
	printf("enter the length of rectangle =");
	scanf("%d",&l);
	
	printf("enter the width of rectangle =");
	scanf("%d",&w);
	
	area = l*w;
	printf("enter the arae of rectangle = %d",area);
	
}
